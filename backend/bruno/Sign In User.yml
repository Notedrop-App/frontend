info:
  name: Sign In User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/signin
  body:
    type: json
    data: |-
      {
        "email": "bartosz.krawczyk34@gmail.com",
        "password": "Mikadeusz2015!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
