info:
  name: Join Waitlist
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/waitlist/join
  body:
    type: json
    data: |-
      {
        "email": "put your email here."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
