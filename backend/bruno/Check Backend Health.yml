info:
  name: Check Backend Health
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
